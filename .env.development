# ── Loaded by: vite dev  |  vite build --mode development ────────────────────
# This file is committed. DO NOT add secrets here.
# Add secrets (VITE_SENTRY_DSN, etc.) to .env.local (git-ignored).

VITE_APP_ENV=development
VITE_SENTRY_TRACE_SAMPLE_RATE=1

# VITE_SENTRY_DSN → set in .env.local for local Sentry reporting (optional)
# VITE_SENTRY_RELEASE → injected at build time; leave empty locally
